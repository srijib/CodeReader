package com.wkswind.codereader.database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DOC_TYPE".
*/
public class DocTypeDao extends AbstractDao<com.wkswind.codereader.database.DocType, Long> {

    public static final String TABLENAME = "DOC_TYPE";

    /**
     * Properties of entity DocType.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Extensions = new Property(0, String.class, "extensions", false, "EXTENSIONS");
        public final static Property Id = new Property(1, Long.class, "id", true, "_id");
        public final static Property Status = new Property(2, Integer.class, "status", false, "STATUS");
        public final static Property Remark = new Property(3, String.class, "remark", false, "REMARK");
    };


    public DocTypeDao(DaoConfig config) {
        super(config);
    }
    
    public DocTypeDao(DaoConfig config, com.wkswind.codereader.database.DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DOC_TYPE\" (" + //
                "\"EXTENSIONS\" TEXT," + // 0: extensions
                "\"_id\" INTEGER PRIMARY KEY ," + // 1: id
                "\"STATUS\" INTEGER," + // 2: status
                "\"REMARK\" TEXT);"); // 3: remark
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DOC_TYPE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, com.wkswind.codereader.database.DocType entity) {
        stmt.clearBindings();
 
        String extensions = entity.getExtensions();
        if (extensions != null) {
            stmt.bindString(1, extensions);
        }
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(2, id);
        }
 
        Integer status = entity.getStatus();
        if (status != null) {
            stmt.bindLong(3, status);
        }
 
        String remark = entity.getRemark();
        if (remark != null) {
            stmt.bindString(4, remark);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1);
    }    

    /** @inheritdoc */
    @Override
    public com.wkswind.codereader.database.DocType readEntity(Cursor cursor, int offset) {
        com.wkswind.codereader.database.DocType entity = new com.wkswind.codereader.database.DocType( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // extensions
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // id
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // status
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // remark
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, com.wkswind.codereader.database.DocType entity, int offset) {
        entity.setExtensions(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setStatus(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setRemark(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(com.wkswind.codereader.database.DocType entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(com.wkswind.codereader.database.DocType entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
